name: Lint
on:
  push:
    paths:
      - 'testing/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: npm install
    - name: Run prettier
      run: npx lint:prettier
    - name: Run eslint
      run: npx lint:eslint
